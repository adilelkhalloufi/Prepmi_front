import { join } from "path";

export const webRoutes = {
  home: '/',
  login: '/login',
  logout: '/logout',
  register: '/register',
  checkout: '/checkout',
  dashboard: '/dashboard',
  menu: '/menu',
  join_now: '/join-now',
  single_product: '/product/:id',


};
